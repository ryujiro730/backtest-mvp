[build]
builder = "DOCKERFILE"
dockerfilePath = "infra/Dockerfile.worker"

[deploy]
startCommand = "sh -c 'celery -A worker.tasks worker -l info --pool=solo'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

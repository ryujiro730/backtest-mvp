[build]
builder = "DOCKERFILE"
dockerfilePath = "infra/Dockerfile.worker"

[deploy]
startCommand = "sh -c 'celery -A worker.tasks worker -l info --concurrency 2'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

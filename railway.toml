# railway.toml — Worker service config
# Worker service: Root Directory = / (empty)

[build]
builder = "DOCKERFILE"
dockerfilePath = "infra/Dockerfile.worker"

[deploy]
startCommand = "celery -A worker.tasks worker -l info --concurrency=4"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
